web:
  build: .
  ports:
    - "80:80"
  links:
    - db
    - redis
  environment:
    - RAILS_ENV
  volumes:
    - public/assets:/home/app/webapp/public/assets
db:
  image: postgres
  ports:
    - "5432"
  volumes:
    - pg/data/:/var/lib/postgresql/data
redis:
  image: redis